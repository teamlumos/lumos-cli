FROM public.ecr.aws/lumos/devcontainer/base:latest

# Set environment variables for proper terminal functionality
ENV TERM=xterm-256color
ENV DEBIAN_FRONTEND=noninteractive

# Ensure proper shell environment
ENV SHELL=/bin/zsh
